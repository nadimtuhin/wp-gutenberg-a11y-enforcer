# WP Gutenberg A11y Enforcer

WordPress Gutenberg block validation enforcer plugin verifying accessibility compliance during block save action.

## Run Tests

```bash
composer install
./vendor/bin/phpunit
```
